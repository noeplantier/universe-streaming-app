[build]
  command = "npm install --force && npx expo export"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"
  # Souvent requis par Expo web en production
  EXPO_WEB_DEBUG = "true"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200